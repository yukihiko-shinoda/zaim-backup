name: Test
permissions:
  contents: read
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
permissions:
  contents: read
  packages: read
jobs:
  call-workflow-test:
    uses: yukihiko-shinoda/invoke-lint/.github/workflows/reusable-test.yml@main
    with:
      support-python-versions: "[ '3.13', '3.12', '3.11', '3.10' ]"
  call-workflow-lint:
    uses: yukihiko-shinoda/invoke-lint/.github/workflows/reusable-lint.yml@main
    with:
      enable-xenon: false
